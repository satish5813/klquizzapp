# Copy to admin-desktop/.env to auto-connect the desktop app (skip the login screen).
# The .env file is git-ignored so your token stays off GitHub.
VITE_API_URL=http://your-api-domain
VITE_ADMIN_TOKEN=your-admin-token
